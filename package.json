{
  "name": "emptysock-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for the EmptySock game engine",
  "keywords": ["emptysock", "mcp", "game-engine", "model-context-protocol"],
  "repository": { "type": "git", "url": "https://github.com/eleferrets/emptysock-mcp" },
  "type": "module",
  "bin": { "emptysock-mcp": "./dist/server.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "tsx": "^4.7.0",
    "vitest": "^5.0.0",
    "@types/node": "^22.0.0"
  },
  "engines": { "node": ">=20" }
}
